# test
on:
  push:
    branches:
      - main
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - run: npm ci
  build:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: "Build application"
        run: npm run build
  # test:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - run: ./test_server.sh
  #   Build:
  #     name: "Build"
  #     runs-on: ubuntu-latest
  #     steps:
  #       - uses: actions/checkout@v2
  #       - run: npm ci && npm run build
  #   Test:
  #     name: "Test"
  #     runs-on: ubuntu-latest
  #       steps:
  #         - uses: actions/checkout@v2
  #         - run: npm ci && npm run build
